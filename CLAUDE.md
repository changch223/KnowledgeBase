<!-- SPECKIT START -->
Active features in flight:
- spec 001-008 — ✅ 実装 + main マージ済 (spec 001-005 commit `0fad9fd` / spec 006 `74d167b` / spec 007 `640c89c` / spec 008 `8f3ce4a` + hot-fix `fbcde69`).
- spec 009-010 — ✅ 実装 + commit `adc2221` (BGTaskScheduler incremental save + 階層的 chunked summarization).
- spec 011 — UI リブランディング + AI ブレインタブ追加 — ✅ 実装 + commit `8b8671e` (本ブランチ `011-ai-brain-tab`、未マージ)。Tab 化 / PowerGauge / KnowledgeMap (Canvas + force-directed) / RecentActivity / 知積リブランディング 全部完成。Unit テスト 18/18 PASS。実機検証 (quickstart.md SC-001〜SC-008) と Instruments 60fps 計測のみ未実施。
- spec 012 — タグ自動付与 (AI Auto-Tag) — ✅ 実装 + commit `0e6e299` (本ブランチ `012-auto-tag`、未マージ)。AutoTagApplier 純粋関数 + KnowledgeExtractionService の hook 2 箇所 + bootstrap で TagStore inject。新 schema ゼロ。Unit テスト 7/7 PASS、既存テスト全回帰 PASS。
- spec 013 — 既存記事への auto-tag backfill — ✅ 実装 + commit `dc877bd` + main マージ済 (PR #2 / merge `47a9338`)。AutoTagBackfillRunner + BackfillFlagStore + ProcessingMonitor.Phase `.tagBackfilling`。Unit テスト 7/7 PASS。
- spec 014 — 統一デザインシステム + Phase 3/4 視覚改善 — ✅ 実装 + commit `b78c2f4` + PR #3 OPEN (本ブランチ `014-design-system`)。
- spec 015 — AI ブレイン v2 + DesignSystem migration + Category 階層 — ✅ 実装 (本ブランチ `015-ai-brain-v2-categories`、未 commit)。AIBrainView v2 (Stats Row + Insight Card + Category List) + DESIGN.md target に DesignSystem 移行 (9 token alias 残し + 5 token 追加) + Tag.categoryRaw lightweight migration + AutoCategoryClassifier + AutoCategoryBackfillRunner + ProcessingMonitor.Phase `.categoryClassifying` + BottomStatusBar phase tint actionBlue 統一。Unit テスト 12/12 PASS、既存テスト全回帰 PASS。実機検証で B1 バグ + 4 UX 要望が判明 → spec 016 へ。
- spec 016 — Category 詳細画面 + ArticleRow 時間軸 + 本文折りたたみ — ✅ 実装 + main マージ済 (PR #4 / merge `66ab948`)。CategoryFilteredListView 新設 + CategoryFilter 純関数 enum で B1 バグ根本解決 (タップ先 = Category 全 Tag union 記事一覧、数字 = 実体一致) + タグフィルター OR (上位 5 + 「+N ▼」展開) + ArticleRow に SavedAtFormatter 時間軸表示 (今日/昨日/N 日前/絶対) + ArticleDetailView bodySection を DisclosureGroup 折りたたみ + KnowledgeCategoryRow.topTagName 削除。Unit テスト 15/15 PASS。実機検証 SC-001/002/003/006/008 ✅、SC-004/005/007 は次回検証。
- spec 017 — Dark/Light Mode 自動切り替え対応 — ✅ 実装 (本ブランチ `018-knowledge-clip-tab` 内に内包、未 commit)。DesignSystem.swift に `Color.adaptive(light:dark:)` extension 新設 + 5 tokens を adaptive 化。Unit テスト 7/7 PASS。実機検証 SC-001〜SC-009 未実施 (まとめて後で)。
- spec 018 — 知識 Clip タブ + Category 統合 AI ダイジェスト + Category 知識総まとめ詳細画面 — ✅ 実装 + main マージ済 (PR #5 / merge `9c41d60`)。新タブ「知識 Clip」(`lightbulb.fill`) + KnowledgeDigest @Model + KnowledgeDigestService (Foundation + Fallback) + 3 view + markStale hook + 起動時 regenerateAllStale。Unit テスト 10/10 PASS。実機検証は基本 ✅、ブラッシュアップは spec 023+ 候補で別途。
- spec 019 — Chrome 連携 (App Intents + iOS Shortcut + 設定画面 Setup Guide) — 🔧 実装中 (本ブランチ `019-chrome-app-intent`、未 commit)。T001-T008 完成、Build SUCCEEDED。残: T012 全テスト回帰 + T013 CLAUDE.md + T014 実機検証 (ユーザー)。
- spec 020 — Safari Web Extension + 自動保存モード — 🔧 実装中 (本ブランチ `019-chrome-app-intent` 継続、未 commit)。Safari Web Extension target 追加 (Apple template ベース) + manifest.json (`*://*/*` 全ページ content_scripts) + content.js (DOM 抽出 + 8 種 blacklist + 滞在時間遅延 immediate/5/10/30 秒) + background.js (toolbar tap で即時保存) + SafariWebExtensionHandler.swift (saveURL + getAutoSaveSettings handlers) + SafariSetupView (3 ステップ + 自動保存 Toggle + 遅延 Picker + 確認 alert) + SettingsView 拡張 (Chrome / Safari 2 エントリ) + 11 ファイル分の Target Membership pbxproj 自動編集。Build SUCCEEDED、scheme test action は Safari target 追加で破損 (要再構成、別途対応)。実機検証未実施。
- spec 021 — AI Chat (RAG) — ✅ 全実装完了 (本ブランチ `019-chrome-app-intent` 継続、未 commit、2026-05-06)。Phase 1-8 (T001-T024) 完成、T025 実機検証のみ未実施。
  - **Phase 1-2 (T001-T008)**: xcstrings 15 文言 + SharedSchema 拡張 + ChatSession/ChatMessage @Model + Article.essenceEmbedding (`@Attribute(.externalStorage) Data?`) lightweight migration + [Float]↔Data zero-copy ext + EmbeddingService (NLEmbedding.sentenceEmbedding(for: .japanese) + Accelerate vDSP_dotpr で L2 正規化 cosine similarity)。**EmbeddingServiceTests 6/6 PASS**
  - **Phase 3 (T009-T012)**: ChatAnswerOutput @Generable + ChatService protocol+実装 (3 段階 availability 分岐 / 50 件 FIFO / 3 段階ハルシネーション post-process: low-similarity 早期 return / cited 空 → 「分かりません」上書き / 存在しない ID filter) + KnowledgeExtractionService に embedding 生成 hook 追加 (単一 + chunked 両パス)。**ChatServiceTests 8/8 PASS**
  - **Phase 4-5 (T013-T017)**: ChatMessageRow (user 右寄せ actionBlue / assistant 左寄せ dsCardBackground + 引用 DisclosureGroup) + ChatInputField (1〜4 行 vertical TextField + 送信 Button) + ChatTabView (.task で session 復元 + LazyVStack messages + ScrollViewReader auto scroll + NavigationDestination → ArticleDetailView)
  - **Phase 6 (T018-T019)**: Fallback 経路 (Embedding 不可 → keyword マッチ retrieval / FM 不可 → essence + KeyFact 並べ整形)
  - **Phase 7 (T020-T021)**: SettingsView に「チャット履歴を全削除」エントリ + 確認 alert + ChatService.deleteAllSessions
  - **Phase 8 (T022-T024)**: 4 タブ目「AI チャット」(`bubble.left.and.bubble.right.fill`) + Build SUCCEEDED + シリアル実行で全テスト PASS
  - **解決済 (2026-05-16)**: BodyExtractorTests/extractsFromMainTag + extractsByDensityScoringWhenNoSemanticTag の deterministic fail は、誤診断だった (「global state mutation」「順序依存」ではない)。真の原因は **テストフィクスチャ自体が BodyExtractor の 100 字閾値を超えられない** こと。`mainTagHTML` の本文は htmlToText 後 79 字、`noSemanticHTML` は density score が 200 閾値に届かなかった。`-only-testing` の identifier が paren 無しだとテストが選択されず "実行されないまま SUCCEEDED 扱い" になっていたため isolated PASS と誤認していた。修正は **フィクスチャ拡張のみ** (BodyExtractor 本体は無改修)。全 9/9 PASS。
  - **scheme test action 復元 (2026-05-06 完了)**: `KnowledgeTree.xcscheme` を xcshareddata に復元、`xcodebuild test -scheme KnowledgeTree` で動作確認済 (ShareExtension scheme 経由は引き続き動作)。次セッションから両 scheme 利用可能
  - **2026-05-06 実機検証で判明 → fix 済**: (1) ChatTabView が @State currentSession.messages を読んでいて SwiftData @Relationship 追加を reactive 検知できず画面更新されない問題 → @Query + sessionID filter に変更、(2) prompt 通り answer 本文に UUID を書く LM の挙動 → prompt に「本文に ID を書くな」明示 + ChatService.stripUUIDsFromBody post-process 追加 (テスト 3 件追加で計 ChatServiceTests 11/11 PASS)、(3) keyboard 閉じない問題 → `.scrollDismissesKeyboard(.interactively)` 追加 (iMessage 風)、(4) **致命**: 履歴全削除後に質問送信すると履歴が部分復活 + 画面フリーズ → currentSession を `@State` ではなく `@Query allSessions` から動的算出 (`pinnedSessionID` + 最新 fallback) に変更、dead reference 問題を根本解決
  - **残**: T025 実機検証 (quickstart 12 シナリオ) ユーザー実施
  - **次世代要望 (spec 033+ 候補)**: モダン Chat UI (Gemini/Claude/ChatGPT 風) — 履歴サイドバー + multi-turn context + token streaming + 引用 inline link 化、大規模 ~600 行、別 spec で実施
- spec 022 — ArticleRow swipe 削除 — ✅ 部分実装完了 (本ブランチ `019-chrome-app-intent` 継続、main 未マージ、2026-05-06)。List 系 3 view 完了: `ArticleListView` (spec 001 から既実装) + `TagFilteredListView` + `EntityFilteredListView`。swipe 方向は `.trailing` (iOS 標準)。LazyVStack 系 2 view は spec 030 で対応予定。Build SUCCEEDED。
- spec 030 — LazyVStack 系 view の削除手段 (Category 詳細 / 知識 Clip 詳細) — 📝 specify+plan 完了 (本ブランチ、未 commit、2026-05-06)。contextMenu (長押し → メニュー) 採用、~30 行極小、新規ファイルゼロ、Phase 1 必須 + Phase 2 (List 系 UX 統合) optional。実装はユーザー判断後に着手。
- spec 034 — PDF サポート (内部 metadata + 本文抽出) — ✅ main マージ済 (PR #7、commit `06cc2a9`、2026-05-06)。PDFKit で title / subject / author / 全 page text を抽出、擬似 HTML 化して既存フローに乗せる。PDFFetcherTests 10/10 PASS。
- **VISION.md 確定版** (本ブランチ `vision-spec-035-038`、未 commit、2026-05-08) — プロダクト戦略を 17 質問のユーザー対話で固める。「読んだ知識を AI が自動で体系化・更新し、必要な時だけ開けば最新の自分が見える、優しい第二の脳」を一文ビジョン、6 設計原則、4 タブ構成、4 機能 (X/Y/Z/W) を確定。
- spec 035 — 「最近のあなた」差分ダイジェスト (機能 X) — ✅ 実装完了 (本ブランチ、未マージ、2026-05-08)。LastOpenedStore + RecentDigestService + RecentDigestSection 新規 + KnowledgeClipView 改修 + KnowledgeTreeApp で起動時 default = .knowledgeClip。**RecentDigestServiceTests 6/6 PASS**。
- spec 036 — 動的トピック自動発見 (機能 Y) — ✅ 実装完了 (本ブランチ、未マージ、2026-05-08)。UserTopic @Model + TopicClusteringService (K-means + Accelerate vDSP) + AI 命名 + Fallback + DynamicTopicsSection + UserTopicCandidateRow + UserTopicDetailView。起動時 + 7 日 batch、minArticles=10/minClusterSize=3/maxK=20。**TopicClusteringServiceTests 6/6 PASS**。
- spec 037 — 時系列事実上書き提案 (機能 Z) — ✅ 実装完了 (本ブランチ、未マージ、2026-05-08)。ConflictProposal @Model + Article.isObsolete 追加 + ConflictDetectionService + KnowledgeExtractionService に hook 追加 (単一+chunked) + FactConflictsSection + ConflictProposalRow。**ConflictDetectionServiceTests 7/7 PASS**。
- spec 024 — Tag 編集 / 統合 / 削除 UI — ✅ 実装完了 (本ブランチ、未マージ、2026-05-08)。TagStore.rename / merge / delete 拡張 + TagManagementView + TagEditSheet + SettingsView エントリ。AI Auto-Tag (spec 012) の誤訂正手段、VISION「AI 自動 + ユーザー確認」原則の完成形。**TagStoreEditTests 8/8 PASS**、xcstrings 19 文言追加。
- spec 040 — Knowledge Graph 抽出 + Digest + RAG 統合 (Phase A) — ✅ 実装完了 (本ブランチ、未 commit、2026-05-16)。Phase 1-7 全て完成。GraphNode/GraphEdge @Model + SharedSchema 拡張 + pbxproj に Share/Safari Extension target 登録 + GraphTripleOutput @Generable + MockLanguageModelSession 拡張 + GraphExtractionService (AI triple → upsert / 30 node 上限 deactivate / 確信度 3 段階 / Fallback 共起) + GraphTraversalService (resolveNodes/neighbors/topByDegree) + KnowledgeExtractionService に extractGraphIfPossible hook (single + chunked) + KnowledgeDigestService prompt に「## このカテゴリーの主要エンティティと関係性」セクション + ChatService prompt に「## 関連エンティティ」セクション (top-k 記事 entity → 1-hop neighbor) + ServiceContainer + KnowledgeTreeApp bootstrap 配線。**Unit テスト全 PASS**: GraphExtractionServiceTests 10/10 + GraphTraversalServiceTests 5/5 + ChatServiceTests 18/18 (新規 graph 統合 3 件含む) + KnowledgeExtractorTests 6/6。Build SUCCEEDED (iPhone 17 Simulator)。BodyExtractorTests 2 件 fail は **既存 bug** (spec 021 既知、HEAD revert しても再現、spec 040 と無関係)。残: 実機検証 (quickstart 未作成、Phase B = spec 041 で UI 検証時に併せて実施)。
- spec 044 — 検索 relevance ranking + matched field — ✅ 実装完了 (本ブランチ、未 commit、2026-05-16)。spec 008 SearchPredicate は既に 8 フィールド substring match を提供していたが結果は savedAt 順のみで title 一致が下位に埋もれていた。本 spec で SearchService 純関数を追加 (title 完全一致 100 / title 部分 50 / entity・tag 20 / essence・summary 10 / canonicalTitle・KeyFact 5、同 score は savedAt desc tiebreak) + MatchField enum (title/essence/summary/keyFact/entity/tag) + ArticleListView.filteredArticles を SearchService 経由に切替。**Unit テスト全 PASS**: SearchServiceTests 5/5 (空 query / title 完全 > 部分 / entity > essence / tag match in fields / savedAt tiebreak)。Build SUCCEEDED + 全 unit suite regression なし (BodyExtractor 既知 bug も同セッションで解消済)。MVP では ArticleRow badge は未実装 (MatchField.localizationKey は将来 spec 用に残置)。
- spec 042 — 英語記事の翻訳前処理 (KnowledgeExtractor) — ✅ 実装完了 (本ブランチ、未 commit、2026-05-16)。原因: 全パイプライン (prompt / @Generable Guide / NLEmbedding.japanese / Auto-Category / Digest / Conflict / Graph) が日本語固定で、英語記事は essence truncate / hallucination / RAG retrieval 不能と多段で連鎖失敗。解決策: 入口翻訳で本文を日本語化 → 既存パイプライン全レイヤを無改修で再利用。実装: 新規 `LanguageDetector.swift` (純関数、NLLanguageRecognizer 経由) + `LanguageModelSessionProtocol.generateTranslation(prompt:)` 追加 (plain String 返却、Generable 不要) + FoundationModelLanguageModelSession 実装 + MockLanguageModelSession 拡張 + `KnowledgeExtractor.prepareForExtraction` (英語のみ翻訳、失敗 / 空 / 1/4 未満は raw fallback) + `buildTranslationPrompt` (固有名詞は英語維持指示) + extract / extractFromChunk 経路 hook。保存ポリシー: title / ArticleBody.body は英語のまま、知識層 (essence / KeyFact / entities) は日本語化。schema 変更ゼロ。**Unit テスト全 PASS**: LanguageDetectorTests 3/3 + KnowledgeExtractorTests 9/9 (新規 3: 日本語スキップ / 英語翻訳 + 訳出本文で抽出 / 翻訳 throws → raw fallback)。Build SUCCEEDED。残: 実機検証 (英語記事 BBC / Wired / TechCrunch 等で SC-1〜SC-5)。
- spec 041 — Knowledge Graph UI + 編集 (Phase B) — ✅ 実装完了 (本ブランチ、未 commit、2026-05-16)。段階 1-5 全て完成。Settings の Graph 表示 toggle (default OFF、`@AppStorage("settings.graphVisible")`) + CategoryGraphView (SwiftUI Canvas、中心 = degree 最大 / 周辺 = 円形配置 / 実線 = label 付き / 破線 = 共起 / 薄色 = isUncertain) + GraphLayout 純関数 + GraphNodeDetailView (関連記事 + edges + 編集 toolbar) + GraphNodeEditSheet (rename/merge/delete + 確認 alert、TagStore 同パターン) + GraphEdgeEditSheet (label rename/delete) + GraphProposalsSection (知識 Clip タブで isUncertain edge を採用/却下/ラベル変更) + GraphProposalReviewService + ConflictDetectionService.detectGraphConflicts (同 source+predicate 複数 target → ConflictProposal 作成、graphEdgeID 付き、ConflictProposal に optional `graphEdgeID: UUID?` lightweight migration) + CategoryFilteredListView / CategoryKnowledgeDetailView に CategoryGraphView 連動表示 + node tap → GraphNodeDetailDestinationLoader push 遷移。**Unit テスト全 PASS**: GraphLayoutTests 3/3 + GraphNodeStoreTests 7/7 + GraphProposalReviewServiceTests 3/3 + ConflictDetectionServiceTests 9/9 (新規 graph 2 件含む)。Build SUCCEEDED (iPhone 17 Simulator)。BodyExtractorTests 2 件 fail は **既存 bug** (spec 021 既知、HEAD revert しても再現)。残: 実機検証 (spec 040 と併せて)。
- spec 033 — AI Chat モダン UI 刷新 — ✅ 実装完了 (本ブランチ、未マージ、2026-05-08)。NavigationSplitView (iPad sidebar / iPhone overlay) + ChatHistorySidebar + ChatSessionRow (相対時刻 + アクティブハイライト + swipe 削除 + 「+ 新しいチャット」) + multi-turn context (直前 4 message) + 擬似 streaming (15ms/文字、Apple Intelligence streaming API 不要) + inline 引用 link (`[タイトル](article-id://UUID)` を AttributedString で描画) + session 個別削除。**ChatServiceTests 15/15 PASS** (新規 4 ケース: multi-turn / deleteSession / inline link prompt / strip preserves link)。R1 streaming API は MVP では擬似 streaming で割り切り、真 streaming は将来 spec。
- spec 038 — 用語のやさしさ整理 (機能 W) — 📝 specify+plan 完了 (本ブランチ、未 commit、2026-05-08)。KeyFact → 事実、entity → 人物・場所・モノ、Category → 分野、Auto-Tag → AI タグ等、xcstrings value 修正 + DESIGN.md Vocabulary 追加、コード型名は維持、~200-300 行。
- spec 019 撤回 — ✅ 撤回完了 (2026-05-06)。Chrome 自動化 (Setup Guide / Personal Automation 連携) は Chrome iOS の x-callback-url が「現在のタブ URL」を返さない技術制約で実用化不可と判明。SettingsView から Chrome エントリ削除 + `ChromeShortcutSetupView.swift` 削除。Chrome は Share Extension (spec 001) のみで運用、ユーザー判断による方針確定。AppIntent / AppShortcutsProvider / ArticleSavingActor 実装は Safari Web Extension が依存するため残置。
- spec 042 — ConceptPage (iKnow V1 Phase A 第 1 弾) — ✅ 実装完了 + 実機検証 PASS + main マージ済 (PR #10 / merge `5ad4960`、2026-05-23)。
- spec 043 — SavedAnswer (AI Chat 答えの永続化と概念ページへの紐付け / iKnow V1 Phase A 第 2 弾) — ✅ 実装完了 + 実機検証 PASS + main マージ済 (PR #11 / merge `a6257ab`、2026-05-23)。
- spec 058 — Auto-Lint + Schema 外出し + Confirm UX 廃止 — 🔧 **Phase A+B+C+D+E 全実装完了** (本ブランチ `056-uiux-redesign-v3` 内に内包、未 commit、2026-05-24)。Autoresearch + LLM Wiki のチューニング思想を iKnow に完全移植。「ユーザーに聞かず、AI が裏で勝手に整理する」が本質。Phase A 完成: Confirm UX 廃止 (ConflictProposal `autoResolved` status + AI 自動採用 / ActionItemsReviewView 削除 / FollowingPeopleSection ⚠️ badge 削除 / ConflictHistoryDisclosure 新規 = ArticleDetailView 末尾「過去の見解 (N) ▼」DisclosureGroup)。Phase B 完成: LintEngine 6 step state machine の 5 step (merge / delete ConceptPage / delete Tag / link / reclassify)、LintLog @Model 永続化、Levenshtein 編集距離 ≤ 2 OR 名前完全一致 merge、60 日無参照 + 関連 ≤ 1 件 + 非 follow 削除、孤立 ConceptPage に link 追加 (max 5)、AutoCategoryClassifier 経由 Tag 再分類。新規 3 ファイル (Models/LintLog.swift + Services/LintEngine.swift + Views/ConflictHistoryDisclosure.swift) + 改修 4 ファイル (ConflictDetectionService.swift auto-resolve / ConflictProposal.swift autoResolved case 追加 / FollowingPeopleSection ⚠️ badge 削除 + ActionItemBadgeData 削除 / KnowledgeClipView navigationDestination 削除 / ArticleDetailView ConflictHistoryDisclosure 配線 / SharedSchema LintLog.self / pbxproj LintLog.swift を Share+Widget extension target 追加) + 削除 1 ファイル (ActionItemsReviewView.swift)。**Unit テスト全 PASS** (LintEngineTests 10 ケース新規 + ConflictDetectionServiceTests 更新 + 既存全 regression)。Build SUCCEEDED (iPhone 17 Simulator、clean、warning ゼロ)。**Phase C 追加**: LintEngine Step 6 (SavedAnswer auto-refresh、ChatService.send 経由 agent loop で最大 3 件/週 isStale 再生成) + 週 1 BGTask (日曜 3 AM、`app.KnowledgeTree.weeklyLint` identifier、Info.plist + handler register + chain submit)。**Phase D 追加**: HealthScoreService (Protocol + Default、孤立 ConceptPage + pending ConflictProposal 合算スコア) + HealthScoreCard / LintLogSection / LintNowButton (60 秒 debounce + 完了 alert) + SettingsView 最上部統合 + HealthScoreServiceTests 4 ケース。**Phase E 追加**: docs/iknow-schema.md 新規 (Autoresearch `program.md` 相当、~250 行で NEVER STOP loop / AgentAction prompt / Conflict rule / Lint merge / Health formula / Hedge phrases / Suggested prompts を集約) + SchemaLoader (Foundation Bundle API + memory cache + code fallback、production 安全保証) + KnowledgeTreeApp.init で `SchemaLoader.shared.load()` + `section(named:)` markdown 解析 + SchemaLoaderTests 4 ケース。**全 Phase で Build SUCCEEDED + 全テスト PASS** (新規 LintEngineTests 10 + HealthScoreServiceTests 4 + SchemaLoaderTests 4 = 18 ケース + 既存全 regression)。残: Phase F (実機検証 quickstart.md SC-001〜SC-018、ユーザー)。
- spec 057 — Agentic Chat (LLM が考えて聞いて調べて答える、ChatGPT 風) — 🔧 **実装完了 + Phase B 強化** (本ブランチ `056-uiux-redesign-v3` 内に内包、未 commit、2026-05-24)。5 ラウンドの対話で「LLM が intent を判断 → tool 呼出 → 答え生成」の agentic loop を確定 → 既存 ChatService 内部 refactor (public API 維持) + 「分かりません」廃止 (HedgePhraseFilter post-process) + clarification chips + long press menu (保存/コピー/共有) + **max 3 round clarification** (連続 clarification 3 回後 forceFinalAnswer 強制) + **AgenticChatServiceTests 12 ケース**。新規 6 ファイル (AgentAction Generable struct + HedgePhraseFilter 純関数 + ClarificationChipsView + AnswerActionsMenu + AgentActionTests + HedgePhraseFilterTests + AgenticChatServiceTests) + 改修 6 ファイル (ChatService internal agent loop + max 3 round / LanguageModelSessionProtocol.generateAgentAction / ChatMessage.clarificationSuggestions + 永続化 / ChatMessageRow long press + chips 配線 / ChatTabView clarification tap 受信 + SavedAnswerService.saveExplicit + auto-save 維持)。Localizable.xcstrings に 3 文言追加 (answer.actions.*)。**Apple Foundation Models 制約対応**: Generable enum 非対応 (UUID + @Guide on case 不可) → Generable struct + actionType String + Swift 側で AgentAction enum 変換パターン (spec 044 同思想)。**ChatService.agentLoopEnabled flag**: default true、既存 ChatServiceTests は false で既存 RAG 経路を継続テスト。**Unit テスト全 PASS** (新規 AgentActionTests 12 + HedgePhraseFilterTests 6 + AgenticChatServiceTests 12 = 30 ケース + 既存全 regression)。Build SUCCEEDED (iPhone 17 Simulator、clean、spec 057 由来 warning ゼロ)。残: 実機検証 (quickstart.md SC-001〜SC-012、ユーザー実施)、spec 056 PR #17 を spec 057 と合わせて update → V3.0 release。
- spec 056 Phase B — Library 日付別 grouping + FAB (`056-uiux-redesign-v3` 内に内包、未 commit、2026-05-24)。LibraryDateGrouper (純関数、既に Phase A で実装済) を活用、`ArticleListView` 内の List を Section + DisclosureGroup 風に日付 grouping (今日 / 昨日 / 今週 / 今月 / それ以前)。新規ファイルゼロ、既存 ArticleListView の List 部分 + ArticleListView root に FAB + AddArticleSheet 配線。Localizable.xcstrings は Phase A で既に追加済。Build SUCCEEDED + 全 unit test PASS。
- spec 056 — UIUX redesign V3.0 (3-tab simplification + Apple News パターン) — 🔧 **実装完了** (本ブランチ `056-uiux-redesign-v3`、未 commit、2026-05-24)。9 ラウンドの対話で「気になったものが、勝手に整理される」を本質と定義 → 4 タブ → 3 タブ (知識 Clip / ライブラリ / AI チャット) + 知識 Clip 8 セクション → 3 セクション (最近の記事 / 続きが気になる / 追っている人物・モノ) に削減 + 旧 root view 削除 (UnderstandingTabView / AIBrainView / Settings tab) + 削除動線を新動線に変更 (アバター → Settings / 続きが気になる → DeepDiveChatView / AI チャット 📊 → Knowledge Graph 全体画面)。新規 12 ファイル + 改修 8 ファイル + 削除 7 ファイル = 純削減で simplification。Localizable.xcstrings に 52 文言追加。**新規 service 3 (RecentArticlesService / SuggestedPromptGenerator / LibraryDateGrouper)、新 transient struct 4 (MixedSurfaceCard / LibraryDateGroup / SuggestedPrompt / ActionItemBadgeData)、UserDefaults 3 keys (cache + V3 migration)**。SwiftData @Model 変更ゼロ (純 UI/Service refactor、CloudKit Production schema deploy 不要)。**Unit テスト全 PASS** (新規 19 + 既存全 regression、serial mode)。Build SUCCEEDED (iPhone 17 Simulator、clean、spec 056 由来 warning ゼロ)。残: 実機検証 (quickstart.md SC-001〜SC-018、ユーザー実施)、commit + V3.0 release。
- spec 051 — iCloud sync (SwiftData CloudKit) — ✅ **Phase A 完成 + main マージ済** (PR #16 / merge `129b348`、2026-05-24)。Phase 0 spike PASS (iOS 26 で App Group + CloudKit private DB 共存確認) → Phase A 実装完了 → 実機検証 PASS → V2.5 release。完成内容: 14 Array `@Relationship` を `[X]?` Optional + `= []` default 化 (Article.tags/digests + 6 inverse / Tag.articles / ConceptPage.relatedArticles / ChatSession.messages / ExtractedKnowledge の 3 配列 / KnowledgeDigest.sourceArticles / SavedAnswer.citedArticles / GraphNode の 3 配列 / UserTopic.articles) + 6 to-one ref Optional 化 (ArticleBody/Enrichment/Knowledge.article、KnowledgeChunkProgress/KeyFact/KnowledgeEntity.knowledge) + Article に 6 missing inverse 追加 (relatedConcepts/conflictsAsNew/conflictsAsOld/graphNodes/savedAnswers/userTopics) + KnowledgeTree.entitlements に `com.apple.developer.icloud-services` + `iCloud.app.KnowledgeTree` container 3 key 追加 + KnowledgeTreeApp.ModelContainer の CloudKit fallback 配線 復活 + SettingsView の iCloud toggle 再開 (confirm alert + 再起動 banner) + ~100 access site を `?? []` で safe unwrap + `?.append` パターン統一 + SwiftData `#Predicate` の `?? []` 非対応箇所 2 件を in-memory filter に切替。**88 ファイル変更 / +3059 / -2623**。Unit テスト全 PASS (serial mode、KnowledgeTreeTests 全 suite)。Build SUCCEEDED (iPhone 17 Simulator、clean、warning ゼロ)。**実機検証 PASS**: アンインストール → 再 install → iCloud sync ON → 全データ復元成功 / CloudKit Dashboard で 10+ CD_* type 自動 deploy 確認。残: 2 台間同期検証 (Phase B 候補)、ストレス test (100+ 件一括 sync / オフライン → オンライン復帰)。
- spec 047 — AI Chat MessageRow に関連 ConceptPage chips 追加 — 🔧 実装完了 (本ブランチ `044-understanding-chat` 内に内包、未 commit、2026-05-24)。AI チャットの assistant 答えで cited articles section 直下に「関連する概念 (N)」chips 行を追加、引用記事と各 ConceptPage.relatedArticles の overlap を in-memory 計算して top 3 を capsule chip 表示、tap で ConceptPageDetailDestination 遷移。chat → concept page の discovery 橋を完成 (Karpathy「概念ページが compound する」原則の片輪)。新規ファイル ゼロ、ChatMessageRow.swift 末尾に `RelatedConceptsChips` private struct (~60 行、@Query + overlap top 3 + FlowingTagsLayout) + ChatMessageRow body 1 行挿入 + ChatTabView に navigationDestination 3 行追加 + xcstrings 1 文言。テスト追加なし (純 UI + @Query)。Build SUCCEEDED + 全 regression suite PASS。残: T007 実機検証 (SC-001〜SC-006、ユーザー)。
- spec 046 — 知識 Clip タブに「確認が必要な答え」セクション追加 (WikiLint Lite Phase 2) — 🔧 実装完了 (本ブランチ `044-understanding-chat` 内に内包、未 commit、2026-05-24)。spec 037 FactConflictsSection の同パターンで `SavedAnswer.isStale=true` を 知識 Clip タブ上部 (FactConflictsSection 直後) に surface。新規 1 ファイル `StaleSavedAnswersSection.swift` (~80 行、@Query で isStale=true + updatedAt desc + 上位 5 + 「+N すべて見る」 NavigationLink → SavedAnswerHistoryView + 0 件で `EmptyView()` calm UX) + 改修 KnowledgeClipView (1 行追加 + navigationDestination 1 行) + xcstrings 2 文言 (「確認が必要な答え」「確認が必要な答え (%lld)」)。テスト追加なし (純 UI + @Query、既存 SavedAnswerServiceTests でカバー)。Build SUCCEEDED + SavedAnswerServiceTests / DeepDiveChatServiceTests / UnderstandingTrackerServiceTests regression PASS。spec 045 で見えるようにした isStale が主タブ (知識 Clip) で発見可能になり、ユーザーが Settings → 履歴まで掘らずとも確認待ちに気づける。残: T006 実機検証 (SC-001〜SC-006、ユーザー)。
- spec 045 — SavedAnswer.isStale 表示 + 「再生成」アクション (WikiLint Lite Phase 1) — 🔧 実装完了 (本ブランチ `044-understanding-chat` 内に内包、未 commit、2026-05-24)。spec 043 で導入した silent な `SavedAnswer.isStale` flag を UI に露出 + 再生成導線を追加。新規ファイル ゼロ、改修 4 view + 1 service + 1 container + 1 app + 1 chat 配線 + 1 xcstrings + 1 test = ~400 行。**追加 method**: `SavedAnswerServiceProtocol.markFresh(_:) throws` + `captureIfWorthyOrReplaceStale(question:answer:citedArticleIDs:sessionID:)` — ChatService.send 末尾 hook を `captureIfWorthy` から `captureIfWorthyOrReplaceStale` に切替 (fresh dup あれば skip / stale dup のみあれば新規 insert + 古を保持 / dup なければ通常 insert)。**追加 UI**: SavedAnswerRow に orange `clock.badge.exclamationmark` icon + 「更新が必要」chip / SavedAnswerDetailView 上部に notice banner + toolbar に「再生成」Button (orange `arrow.clockwise`) + ellipsis menu に「更新済としてマーク」 + 既存 delete を menu 内に移動 / SavedAnswerHistoryView 上部に「⚠️ 更新が必要 (N)」フィルター chip (件数 0 で非表示) + showStaleOnly toggle 動作。**再生成フロー**: `ServiceContainer.pendingRegenerateRequest: PendingRegenerateRequest?` 経由で SavedAnswerDetailView → KnowledgeTreeApp (`.onChange` で `selectedTab = .chat`) → ChatTabView (`.onChange` で新 ChatSession 作成 + question 自動 send) → ChatService.send 末尾 hook → `captureIfWorthyOrReplaceStale` → 新 SavedAnswer auto-save (古は履歴保護で残る、calm UX)。**Unit テスト全 PASS**: SavedAnswerServiceTests 拡張 5 件 (markFresh 2 + captureIfWorthyOrReplaceStale 3) + ChatServiceTests 既存 + 新 hook 検証 PASS + DeepDiveChatService / Understanding 系 regression なし。Build SUCCEEDED (clean、iPhone 17 Simulator)、spec 045 由来 warnings ゼロ。残: T012 実機検証 (SC-001〜SC-010、spec 044 + spec 030 と一緒に)。Constitution Check 全 PASS。
- spec 030 — LazyVStack 系 view の削除手段 (contextMenu 採用) — 🔧 実装完了 (本ブランチ `044-understanding-chat` 内に内包、未 commit、2026-05-23)。Phase 1 (T001-T002 = CategoryFilteredListView / CategoryKnowledgeDetailView への contextMenu + delete helper) は spec 044 着手前に既実装済 (発見的に判明)、Phase 2 (T003-T005 = ArticleListView / TagFilteredListView / EntityFilteredListView に `.swipeActions` の隣に `.contextMenu` 併記) を本セッションで実装。全 5 view で「swipe (List 系) or 長押し (両系) → 削除」の 2 経路で UX 統一。新規ファイル ゼロ、新規 schema ゼロ、新規 service ゼロ、~15 行追記のみ。Build SUCCEEDED + SwiftDataArticleStoreTests 4/4 PASS。残: T008 実機検証 (SC-001〜SC-007、ユーザー)。
- spec 044 — Understanding Chat (家庭教師ループ + 学習タブ / iKnow V1 Phase A 核心ロジック完成) — 🔧 実装完了 + brushup 1 回 (本ブランチ `044-understanding-chat`、未 commit、2026-05-23)。Karpathy「You can outsource your thinking, but you cannot outsource your understanding」の家庭教師ループを実体化、秘書ループ (spec 021+042+043) と並ぶ iKnow V1 の核心メカニズム完成。新タブ「学習」(起動 default、`book.fill`、4 タブ目から 1 番目に昇格) + 5-tier surface scoring (newKnowledge 100 / needsUpdate 90 / shallow 80 / deepDive 60 / review 40、dismissed -10) + DeepDiveChatView (chat body + sticky 3 button bar ✓ わかった / 🤔 もっと / ✗ 違う + 家庭教師調 prompt context 注入) + ConceptPage.userUnderstanding clamp [0, 5] +1 + 1-hop 波及 (ConceptPage.relatedConceptIDs 経由、累積 2 件で +1 round-half-up) + 行動履歴 (UnderstandingInteraction @Model) + ConceptPage 詳細 toolbar「学習する」Button + AI ブレイン軽量学習統計 (P3、0 件で非表示) + tab default migration (UserDefaults `spec044_learningTabMigrated` で 1 回限り)。新規 12 ファイル (UnderstandingInteraction @Model + transient struct 4 つ / UnderstandingCardSurfaceService Protocol+Default / UnderstandingTrackerService Protocol+Default / DeepDiveChatStarter Protocol+Default / UnderstandingTabView / UnderstandingCardRow + LabelBadge / UnderstandingCardListView / DeepDiveChatView + UnderstandingActionBar / UnderstandingStatsSection / 3 test ファイル / 1 UI test ファイル) + 改修 6 ファイル (SharedSchema + 1 行 / ServiceContainer + 3 service / KnowledgeTreeApp .learning タブ + 3 service bootstrap + migration / ConceptPageDetailView toolbar「学習する」Button + navigationDestination / AIBrainView UnderstandingStatsSection 挿入 / Localizable.xcstrings 29 文言) + pbxproj に UnderstandingInteraction.swift を ShareExtension + SafariExtension target にも追加。**Unit テスト全 PASS**: UnderstandingCardSurfaceServiceTests 10/10 (空状態 / newKnowledge 優先 / needsUpdate 優先 / shallow + 関連記事 7d / dismissed -10 / limit=5 / blend ConceptPage+SavedAnswer / 全 max → review fallback / deepDive label / 同 priority tiebreak) + UnderstandingTrackerServiceTests 8/8 (recordUnderstood +1 / max clamp / 1-hop 波及 累積 2 件 +1 / recordNeedMore 不変 + 履歴 / recordDismissed 不変 + 履歴 / SavedAnswer 経由 relatedConceptIDs 全部 +1 / graph 不存在 silent / 連打 6 回で max 5 停止 + 全 6 件履歴) + DeepDiveChatStarterTests 5/5 (MockChatService + MockTracker、ChatSession 作成 + title / tutor prompt に concept name 含む / openedChat 1 件記録 / send throws でも session 返却 / SavedAnswer 経路で question + answer 抜粋)。**Build SUCCEEDED** (iPhone 17 Simulator、clean build、spec 044 由来 warnings ゼロ)。pre-existing flaky UI test 失敗 6 件 (AIBrainTabUITests 5 / SaveArticleUITests 1) + 新規 UnderstandingTabUITests 3 件は Simulator cold-launch 起因で実機検証で判定する性質。残: T027 実機検証 (quickstart.md SC-001〜SC-010 + 既存回帰、ユーザー実施)。Constitution Check 全 PASS。**brushup (実機検証で判明した 2 bug を session 内で fix、2026-05-23)**: (1) 旧 DeepDiveChatStarter が ChatService.send 経由で家庭教師 prompt を user message として送信、画面に system prompt 露出 + (2) ChatService の RAG 経路 (low-similarity early return) で「分かりません」連発 → 新 `DeepDiveChatService.swift` 追加 (Foundation Models 直接呼び、`LanguageModelSessionProtocol.generateTutorReply(prompt:) -> String` 追加、retrieval なし、初回 AI 発話は user_message ゼロで永続化、multi-turn 履歴 prompt 同梱、Apple Intelligence 不可時は ConceptPage.summary fallback)、DeepDiveChatView を新 service 経由に変更 + ChatInputField 追加 + 「家庭教師が考えています…」 spinner + 「✗ 違う」 → 「✗ 興味ない」文言変更 + 3 ボタンの上に hint banner (「✓ 理解した / 🤔 もっと聞く / ✗ 今は表示不要」)。新規 DeepDiveChatServiceTests 7/7 PASS + MockLanguageModelSession 拡張 (`nextTutorReplyResult` / `tutorReplyCallCount`)。既存 30 ケース regression なし。Karpathy LLM Wiki 思想の Compound Moment 条件 1 を実体化。AI Chat 答えに引用 2+ 件 + 50 字+ あれば SavedAnswer @Model として永続化、引用記事 → 関連 ConceptPage (spec 042) を overlap 数で top 5 紐付け、ConceptPage 詳細画面「この概念についての質問と答え (N)」セクション + SavedAnswerDetailView (Live check pattern) + SavedAnswerHistoryView (Settings 配下) + 検索 (P3) を実装。Foundation Models 不要 (純粋ロジック層、AI 部分は spec 021 ChatService)。新規 7 ファイル (SavedAnswer @Model + Hashable destinations / SavedAnswerService Protocol+Default / SavedAnswerRow / SavedAnswerSection / SavedAnswerDetailView / SavedAnswerHistoryView / SavedAnswerServiceTests) + 改修 8 ファイル (SharedSchema / ChatService.ask 末尾 hook + DI / KnowledgeExtractionService extract 末尾 stale hook 2 経路 + DI / ConceptPageStore.merge 連動 source→target 置換 / ServiceContainer / SearchService.searchSavedAnswers + ScoredSavedAnswer / KnowledgeTreeApp bootstrap / ConceptPageDetailView 5 番目セクション配置 / SettingsView NavigationLink) + Localizable.xcstrings ~20 文言。pbxproj に SavedAnswer.swift を ShareExtension + SafariExtension target にも追加 (spec 042 同手順)。**Unit テスト全 PASS**: SavedAnswerServiceTests 10/10 (auto-save 条件分岐 / 重複防止 / relatedConceptIDs 解決 / setPinned/delete / markStaleForArticle 連鎖) + ChatServiceTests 拡張 2 件 (MockSavedAnswerService + hook 検証) + ConceptPageStoreTests 拡張 1 件 (merge 連動 source→target 置換) + 既存全 suite regression なし。**Build SUCCEEDED** (iPhone 17 Simulator、clean build、spec 043 由来 warnings ゼロ)。pre-existing flaky UI test 6 件 (AIBrainTabUITests 5 / SaveArticleUITests 1) は spec 043 と無関係。残: 実機検証 (quickstart.md SC-001〜SC-008、ユーザー実施)。Constitution Check 全 PASS。Karpathy LLM Wiki 思想の「概念ページ」を SwiftData @Model + Foundation Models (`@Generable ConceptSynthesisOutput`) で実装。新規 9 ファイル (ConceptPage @Model / ConceptSynthesisService Protocol+Foundation+Fallback / ConceptPageStore / ConceptPageCard / ConceptPageDetailView / ConceptPageEditSheet / 2 テストファイル / **Info.plist 新規**) + 改修 9 ファイル (SharedSchema / LanguageModelSessionProtocol / KnowledgeExtractionService extract hook 2 経路 / BackgroundExtractionScheduler / **pbxproj に INFOPLIST_FILE 設定 + auto-sync exception** / ServiceContainer / KnowledgeTreeApp bootstrap / KnowledgeClipView / SearchService P3 / ArticleDetailView P3)。**Unit テスト全 PASS**: ConceptSynthesisServiceTests 10/10 + ConceptPageStoreTests 8/8 + KnowledgeExtractionServiceTests 拡張 2 件。**実機検証 完了**: 知識 Clip タブで「あなたが追っている人物・モノ」セクション動作 + 「整理中…」が消える + 詳細画面で 200-400 字 summary 表示確認。**Build SUCCEEDED** (clean build、spec 042 由来 warnings ゼロ)。**実機検証フェーズで判明 + 修正した 5 件 post-implementation hardening**: (1) `INFOPLIST_KEY_BGTaskSchedulerPermittedIdentifiers` に space-separated value を入れたが Xcode が array 展開できず両 BGTask 識別子が消えた → 実 `Info.plist` file + `INFOPLIST_FILE` 設定 + `PBXFileSystemSynchronizedBuildFileExceptionSet` で auto-sync 除外、spec 009 chunked extraction も同時に修復、(2) `processNewArticle` が `isStale=true` 設定までで終わり新規 ConceptPage が次回起動まで summary 出なかった → 末尾に `await resynthesizeAllStale()` 追加 (両 service)、(3) Foundation Models 4096 token 制限頻繁超過 → threshold 5→3、chunk_size 4→2、essence 300→200 chars、KeyFact 3×100→2×60 chars、title 80 chars truncate (新規)、@Guide description 250→50 chars 圧縮、(4) AI が空 summary を返した時に既存 summary が消える bug → defensive code で空 AI 出力時は既存保持、(5) Foundation 失敗で isStale=true 永続ループ → safety net で例外時に Fallback service へ自動委譲。Constitution Check 全 PASS。pre-existing flaky UI test 失敗 8 件は spec 042 と無関係。

Read these first for the current planning context (spec 044 = newest plan):

**spec 044 (Understanding Chat / 家庭教師ループ + 学習タブ / iKnow V1 Phase A 核心ロジック完成)**:
- plan: `specs/044-understanding-chat/plan.md` — 新規 11 + 改修 7 = ~1750 行 / Constitution Check 全 PASS / Phase A 3-4 週間 (最大規模) / ChatService 無改修
- research: `specs/044-understanding-chat/research.md` — R1〜R12 (UnderstandingInteraction @Model / UnderstandingCard transient / 5-tier surface scoring / 1-hop graph 波及 / DeepDiveChatStarter wrapper / LastOpenedStore .learning migration / 学習タブ leftmost 配置 / 統一 Card UI / DeepDiveChatView sticky 3 button bar / ConceptPage 詳細「学習する」Button / SavedAnswer surface / テスト戦略 23 ケース)
- data-model: `specs/044-understanding-chat/data-model.md` — 新 @Model `UnderstandingInteraction` 5 フィールド (Relationship なし孤立 ID 参照) + transient 4 つ (UnderstandingCard / UnderstandingCardKind / UnderstandingCardLabel / UnderstandingCardListDestination) + 既存 @Model 改修ゼロ
- contracts: `specs/044-understanding-chat/contracts/{understanding-interaction-model, understanding-card-transient, understanding-card-surface-service, understanding-tracker-service, deep-dive-chat-starter, understanding-tab-view, deep-dive-chat-view, understanding-card-row, concept-page-detail-learn-button}.md` (9 ファイル)
- quickstart: `specs/044-understanding-chat/quickstart.md` — 10 検証シナリオ (1 秒表示 / chat 3 秒 / ✓ +1 / 1-hop 波及 / 起動 default / 60fps / 空状態 / surface 入れ替わり / streak ゼロ / 統計 0 件で非表示 + 既存回帰)

**spec 043 (SavedAnswer / iKnow V1 第 2 弾)**:
- plan: `specs/043-saved-answer/plan.md` — 新規 7 + 改修 8 = ~1300 行 / Constitution Check 全 PASS / Phase A 2 週 / 純粋ロジック層 (AI 不要)
- research: `specs/043-saved-answer/research.md` — R1〜R10 (@Model 11 フィールド / ChatService.ask hook / SavedAnswerService Protocol+単一実装 / KnowledgeExtractionService stale hook / ConceptPage 紐付け解決 (overlap 数 top 5) / ConceptPageStore.merge 連動 / SavedAnswerSection in ConceptPage 詳細 / SavedAnswerDetailView Live check (spec 042 同パターン) / 履歴画面 (Settings 配下) + 検索 P3 / テスト戦略 8-10 ケース + hook 検証 2-3)
- data-model: `specs/043-saved-answer/data-model.md` — 新 @Model `SavedAnswer` 11 フィールド (片方向 @Relationship.nullify for citedArticles、chatSessionID UUID? 弱結合、relatedConceptIDs [UUID] 弱結合) + transient 3 つ (SavedAnswerDetailDestination / SavedAnswerListByConceptDestination / ScoredSavedAnswer)
- contracts: `specs/043-saved-answer/contracts/{saved-answer-model, saved-answer-service, chat-service-hook, knowledge-extraction-stale-hook, saved-answer-detail-view, saved-answer-section, saved-answer-history-view}.md` (7 ファイル)
- quickstart: `specs/043-saved-answer/quickstart.md` — 8 検証シナリオ (auto-save / 重複防止 / ConceptPage section / 詳細 + Article jump / 60fps 履歴 / ピン+削除 / isStale 連鎖 / 検索 P3 + 既存回帰)

**spec 042 (ConceptPage / iKnow V1 第 1 弾)**:
- plan: `specs/042-concept-page/plan.md` — 新規 8 + 改修 8 = ~1500 行 / Constitution Check 全 PASS / Phase A 3 週
- research: `specs/042-concept-page/research.md` — R1〜R10 (@Model 12 フィールド / extract hook / Foundation+Fallback / @Generable ConceptSynthesisOutput / hierarchical+meta-summary / ConceptPageStore / 知識 Clip セクション + Card / DetailView 4 セクション / BGTask conceptResynthesis / テスト戦略 18+8 ケース)
- data-model: `specs/042-concept-page/data-model.md` — 新 @Model `ConceptPage` 12 フィールド (片方向 @Relationship.nullify、Article 側影響ゼロ) + SharedSchema 拡張 + transient 5 つ (ConceptSynthesisOutput / ConceptSummaryChunk / ConceptPageDetailDestination / ConceptPageListDestination / ConceptPageStoreError)
- contracts: `specs/042-concept-page/contracts/{concept-page-model, concept-synthesis-service, concept-synthesis-output, concept-page-store, concept-page-card, concept-page-detail-view, knowledge-extraction-service-hook}.md` (7 ファイル)
- quickstart: `specs/042-concept-page/quickstart.md` — 10 検証シナリオ (自動生成 / summary / insights / isStale / hierarchical / 編集 / 60fps / Fallback / backfill / Article jump + 既存回帰)

**spec 019 (Chrome 連携 App Intents + iOS Shortcut + 設定画面)**:
- plan: `specs/019-chrome-app-intent/plan.md` — 新規 4 + 改修 2 + 新規テスト 1 = ~7 ファイル / Constitution Check 全 PASS
- research: `specs/019-chrome-app-intent/research.md` — R1〜R12 (AppIntent 構成 / AppShortcutsProvider 自動登録 / ArticleSavingActor SwiftData 経路 / SwiftData lifecycle / SettingsView Form / Step Card UI / 歯車 toolbar / Info.plist 不要 / xcstrings 13 文言 / static performSave テスト戦略 / Personal Automation 「実行前通知 OFF」 / Chrome x-callback-url 制約)
- data-model: `specs/019-chrome-app-intent/data-model.md` — 既存 Article 再利用 + transient AppIntent / Provider / Destination + actor + UserDefaults flag 1 つ
- contracts: `specs/019-chrome-app-intent/contracts/{save-url-to-knowledgetree-intent, app-shortcuts-provider, article-saving-actor, settings-view, chrome-shortcut-setup-view}.md`
- quickstart: `specs/019-chrome-app-intent/quickstart.md` — 12 検証シナリオ (自動登録 / 手動実行 / 重複 / 無効 URL / Personal Automation / 歯車 / SettingsView / Setup Guide / deeplink / Complete-Reset / fallback / 既存回帰)

**spec 018 (知識 Clip タブ + Category 統合 AI ダイジェスト)**:
- plan: `specs/018-knowledge-clip-tab/plan.md` — 新規 6 + 改修 5 + 新規テスト 2 = ~13 ファイル / Constitution Check 全 PASS
- research: `specs/018-knowledge-clip-tab/research.md` — R1〜R12 (KnowledgeDigest @Model / @Generable DigestOutput / Foundation+Fallback service / markStale hook / pull-to-refresh / KnowledgeClipCard layout / 包括サマリ / 期間フィルター / SwiftData migration / テスト戦略 / fallback トリガー / トークン上限)
- data-model: `specs/018-knowledge-clip-tab/data-model.md` — 新 @Model KnowledgeDigest (sourceArticles non-optional、Constitution III) + Article inverse + transient 4 つ
- contracts: `specs/018-knowledge-clip-tab/contracts/{knowledge-digest-model, knowledge-digest-service, knowledge-clip-view, knowledge-clip-card, category-knowledge-detail-view}.md`
- quickstart: `specs/018-knowledge-clip-tab/quickstart.md` — 12 検証シナリオ (新タブ / カード / 期間 / 詳細画面遷移 / stale / refresh / fallback / Empty / マルチカード / 既存回帰)

**spec 017 (Dark/Light Mode 自動切り替え対応)**:
- plan: `specs/017-dark-mode-tokens/plan.md` — DesignSystem.swift 一元 + DESIGN.md 更新 + ColorAdaptiveTests 新規、Constitution Check 全 PASS
- research: `specs/017-dark-mode-tokens/research.md` — R1〜R10 (Color.adaptive 実装方式 / Dark variant 値選定 / opacity auto adapt / 9 alias 経由 / テスト戦略 / DESIGN.md 更新範囲 / iOS 14+ サポート / Reduce Transparency 自動対応)
- data-model: `specs/017-dark-mode-tokens/data-model.md` — 既存 @Model 無関係、Color extension のみ
- contracts: `specs/017-dark-mode-tokens/contracts/color-adaptive.md` — Color.adaptive(light:dark:) 契約 + 7 unit test ケース
- quickstart: `specs/017-dark-mode-tokens/quickstart.md` — 9 検証シナリオ (Light 保持 / Dark 切替 / Auto / 各 view Dark 視覚 / Reduce Transparency / パフォーマンス / 廃止 view)

**spec 016 (Category 詳細画面 + ArticleRow 時間軸 + 本文折りたたみ)**:
- plan: `specs/016-category-detail-view/plan.md` — 新規 1 view + 改修 5 view + Hashable destination 1 つ / Constitution Check 全 PASS
- research: `specs/016-category-detail-view/research.md` — R1〜R10 (destination 配置 / +N 展開 UX / savedAt フォーマット / DisclosureGroup 位置 / Tag 集計 / カウント仕様 / テスト戦略 / xcstrings / navigationDestination / topTagName 削除)
- data-model: `specs/016-category-detail-view/data-model.md` — 既存 @Model 再利用 + transient struct 1 つ (CategoryFilteredDestination)
- contracts: `specs/016-category-detail-view/contracts/{category-filtered-list-view, category-filtered-destination, article-row-saved-at, article-detail-body-disclosure}.md`
- quickstart: `specs/016-category-detail-view/quickstart.md` — 9 検証シナリオ (B1 修正 / OR フィルター / +N 展開 / 60 秒以内反映 / savedAt 表示 / 本文折りたたみ / Reduce Motion / 既存回帰 / unit test)

**spec 015 (AI ブレイン v2 + DesignSystem migration + Category)**:
- plan: `specs/015-ai-brain-v2-categories/plan.md`
- research: `specs/015-ai-brain-v2-categories/research.md` (R1〜R10)
- data-model: `specs/015-ai-brain-v2-categories/data-model.md`
- contracts: `specs/015-ai-brain-v2-categories/contracts/{auto-category-classifier, auto-category-backfill-runner, ai-brain-stats-row, ai-insight-card, knowledge-category-row}.md`
- quickstart: `specs/015-ai-brain-v2-categories/quickstart.md` (12 検証シナリオ)

**spec 013 (既存記事への auto-tag backfill)**:

**spec 013 (既存記事への auto-tag backfill)**:
- plan: `specs/013-auto-tag-backfill/plan.md` — bootstrap 末尾 1 ブロック / 純 UI 拡張 / 新 service 1 つ + protocol 1 つ
- research: `specs/013-auto-tag-backfill/research.md` — R1〜R5 (ProcessingMonitor.Phase 拡張 / UserDefaults キー / MainActor 並行性 / fetch 戦略 / テスト隔離)
- data-model: `specs/013-auto-tag-backfill/data-model.md` — 既存 @Model 再利用 + UserDefaults キー 1 つ + Phase enum 拡張
- contracts: `specs/013-auto-tag-backfill/contracts/{auto-tag-backfill-runner,backfill-flag-store}.md`
- quickstart: `specs/013-auto-tag-backfill/quickstart.md` — 7 検証シナリオ (1 度限り backfill / 2 回目 early return / 100 件 30 秒 / 整理済保持 / 新記事競合 / 強制終了復帰 / 新規インストール)

**spec 012 (タグ自動付与 / AI Auto-Tag)**:
- plan: `specs/012-auto-tag/plan.md` — KnowledgeExtractionService への hook 2 箇所 / 新 service ゼロ / Constitution Check 全 pass
- research: `specs/012-auto-tag/research.md` — R1〜R5 (hook 位置 / API 形 / early return / DI / テスト戦略)
- data-model: `specs/012-auto-tag/data-model.md` — 既存 @Model 再利用、transient struct ゼロ
- contracts: `specs/012-auto-tag/contracts/{auto-tag-applier,knowledge-extraction-service-hook}.md`
- quickstart: `specs/012-auto-tag/quickstart.md` — 7 検証シナリオ (新規 5 タグ付与 / 既存タグ skip / 全削除復活 / 失敗時非付与 / spec 011 波及 / 既存挙動回帰 / 100 件取りこぼし)

**spec 011 (UI リブランディング + AI ブレインタブ)**:

**spec 011 (UI リブランディング + AI ブレインタブ)**:
- plan: `specs/011-ai-brain-tab/plan.md` — TabView 化 / 純 UI 拡張 / 新 @Model ゼロ / Constitution Check 全 pass
- research: `specs/011-ai-brain-tab/research.md` — R1〜R8 (TabView 環境注入、Canvas force-directed、@Query 集計、7 日 predicate、CFBundleDisplayName、エッジ計算、新繋がり判定)
- data-model: `specs/011-ai-brain-tab/data-model.md` — 既存 @Model 再利用 + transient 型 (MapNode / MapEdge / MapGraph / RecentActivitySnapshot)
- contracts: `specs/011-ai-brain-tab/contracts/{ai-brain-view,knowledge-map-builder,power-gauge-card,recent-activity-cards}.md`
- quickstart: `specs/011-ai-brain-tab/quickstart.md` — 7 検証シナリオ (空状態 / カウントアップ / 60fps / live update / 既存回帰 / タブステート保持 / a11y)

**spec 009 / 010 (実装済)**:
- spec 009 plan: `specs/009-background-extraction/plan.md`
- spec 010 plan: `specs/010-hierarchical-summary/plan.md`

For spec 006-008 reference:
- spec 006 plan: `specs/006-chunked-summarize/plan.md`
- spec 007 plan: `specs/007-multipage-fetch/plan.md`
- spec 008 plan: `specs/008-search-tags-graph/plan.md`

For spec 001 〜 005 reference:
- spec 001 plan: `specs/001-save-article/plan.md`
- spec 005 spec + quickstart: `specs/005-detail-status-ui/spec.md` + `quickstart.md`

Project constitution: `.specify/memory/constitution.md` (v1.0.0, 7 Japanese-first
product principles + secondary engineering quality gates).
<!-- SPECKIT END -->
